# test of travis
language: c
os:
  - linux
compiler:
  - gcc
before_install:
  - sudo apt-get -qq update
  - sudo apt-get -qq install cmake cpack texlive gcc flex bison build-essentials etoolbox poppler-utils texlive-latex-extra
script:
  - ./build_src_bin_mcstas 2.3

